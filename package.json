{
  "name": "test-fstack-mono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "infra:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db rustfs tusd",
    "infra:prod": "docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile prod up -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0"
}
